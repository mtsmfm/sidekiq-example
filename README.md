# Sidekiq example with docker (ruby:alpine)

## Getting Started

    docker-compose build
    docker-compose run web bin/setup
    docker-compose up
    # open localhost:9292 on your browser
    docker-compose run web bin/hello_world
